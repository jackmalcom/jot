{
  "name": "@jot/cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "check": "tsc -p tsconfig.json && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260908.1",
    "wrangler": "^4.76.0"
  },
  "overrides": {
    "sharp": "^0.35.4"
  }
}
